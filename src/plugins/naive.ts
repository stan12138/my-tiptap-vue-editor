import {
    create, NButton, NInput, NTag, NIcon, NIconWrapper,
    NGradientText, NGrid, NGridItem, NDivider, NBackTop,
    NMessageProvider, NCard, NList, NListItem, NPagination,
    NDataTable, NModal, NForm, NFormItem, NMenu, NAvatar,
    NDropdown, NImage, NUpload, NInputGroup, NSpin, NRadio,
    NCheckbox, NDatePicker, NProgress, NColorPicker, NPopover,
    NSwitch, NScrollbar, NFlex, NUploadDragger, NText, NSlider
} from "naive-ui";

const naive = create({
    components: [
        NButton, NInput, NGradientText, NGrid, NGridItem, 
        NDivider, NTag, NIcon, NIconWrapper, NBackTop,
        NMessageProvider, NCard, NList, NListItem, NPagination,
        NDataTable, NModal, NForm, NFormItem, NMenu, NAvatar,
        NDropdown, NImage, NUpload, NInputGroup, NSpin, NRadio,
        NCheckbox, NDatePicker, NProgress, NColorPicker, NPopover,
        NSwitch, NScrollbar, NFlex, NUploadDragger, NText, NSlider
    ]
})

export default naive;